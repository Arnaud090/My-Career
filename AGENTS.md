# AGENTS.md

## Build command
```powershell
cd mycareer\frontend; npm run build
```

## Lint command
```powershell
cd mycareer\frontend; npm run lint
```

## Deployment
- Backend: auto-deployed via Render from `backend/` directory
- Frontend: auto-deployed via Render from `frontend/` directory
- Backend URL: https://my-career-fvep.onrender.com
- Frontend URL: (Render auto-generates from static site service)
