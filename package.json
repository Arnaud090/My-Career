{
  "name": "mycareer-00",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "dependencies": {
    "openai": "^4.73.0"
  },
  "engines": {
    "node": "22.x"
  }
}
